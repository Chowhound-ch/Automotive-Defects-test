<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="per.chowhound.hfut.mapper.RecordMapper">
    
    <resultMap type="per.chowhound.hfut.domain.Record" id="RecordResult">
        <result property="recordId"    column="record_id"    />
        <result property="type"    column="type"    />
        <result property="userId"    column="user_id"    />
        <result property="result"    column="result"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="isDelete"    column="is_delete"    />
    </resultMap>

    <sql id="selectRecordVo">
        select record_id, type, result, create_time, update_time, is_delete from record
    </sql>

    <select id="selectRecordList" parameterType="per.chowhound.hfut.domain.Record" resultMap="RecordResult">
        <include refid="selectRecordVo"/>
        <where>  
            <if test="type != null "> and type = #{type}</if>
            <if test="result != null  and result != ''"> and result = #{result}</if>
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="isDelete != null "> and is_delete = #{isDelete}</if>
        </where>
    </select>
    
    <select id="selectRecordByRecordId" parameterType="Long" resultMap="RecordResult">
        <include refid="selectRecordVo"/>
        where record_id = #{recordId}
    </select>

    <insert id="insertRecord" parameterType="per.chowhound.hfut.domain.Record" useGeneratedKeys="true" keyProperty="recordId">
        insert into record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="type != null">type,</if>
            <if test="result != null and result != ''">result,</if>
            <if test="userId != null ">user_id,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="isDelete != null">is_delete,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="type != null">#{type},</if>
            <if test="result != null and result != ''">#{result},</if>
            <if test="userId != null ">#{userId},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="isDelete != null">#{isDelete},</if>
         </trim>
    </insert>

    <update id="updateRecord" parameterType="per.chowhound.hfut.domain.Record">
        update record
        <trim prefix="SET" suffixOverrides=",">
            <if test="type != null">type = #{type},</if>
            <if test="result != null and result != ''">result = #{result},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="isDelete != null">is_delete = #{isDelete},</if>
        </trim>
        where record_id = #{recordId}
    </update>

    <delete id="deleteRecordByRecordId" parameterType="Long">
        delete from record where record_id = #{recordId}
    </delete>

    <delete id="deleteRecordByRecordIds" parameterType="String">
        delete from record where record_id in 
        <foreach item="recordId" collection="array" open="(" separator="," close=")">
            #{recordId}
        </foreach>
    </delete>
</mapper>